# C500开通信息与环境日志表


## A. 租赁前：镜像选择表

```text
【租赁平台】
平台名称：模力方舟
平台页面链接（如有）：

【硬件选项】
GPU完整名称：MetaX C500（沐曦曦云 C500）
单卡显存：64 GB
GPU数量：1
是否整卡独占：是
CPU可选规格：Intel(R) Core(TM) i7-8550U
内存可选规格：128 GB
系统盘可选规格：30 GB
数据盘可选规格：500 GB

【可选镜像】
请把页面里所有与以下关键词有关的镜像完整名称粘贴过来：
MXMACA / PyTorch / mcPyTorch / Triton / vLLM-MetaX / FlagOS
镜像1：vLLM / 0.17.0 / Python 3.10 / maca 3.5.3.307

【访问方式】
是否支持SSH：是
是否支持上传文件：是
是否可以访问GitHub或Gitee：是
计费价格（元/小时）：4
```

已确认镜像为 `vLLM / 0.17.0 / Python 3.10 / maca 3.5.3.307`。整卡独占属性及其他候选镜像未获得页面完整名称，因此保留为空，不作推测。不要只写“最新版”，必须复制完整镜像Tag。

## B. 开通后：实例信息表

```text
【实例信息】
实例名称：未单独保存（C500环境记录目录：c500-e928d862973e-20260722）
开通时间：2026-07-22 17:24（北京时间）
SSH别名：metax-c500
当前工作目录：/data/mxmoe-adapt



> 说明：实例名称、到期时间没有可靠记录，因此没有猜填。SSH地址、端口、用户名和认证信息也不写入公开仓库。

## C. 开通后：只读环境日志

以下为 2026-07-22 C500 实机运行时保存的回显或结构化记录。证据文件为
[`c500-environment-before.json`](../results/c500-e928d862973e-20260722/c500-environment-before.json) 和
[`c500-environment-final.json`](../results/c500-e928d862973e-20260722/c500-environment-final.json)。
未被原始记录完整保存的命令回显会明确标注，不根据经验补写。

```

### C1. GPU日志

命令：

```bash
mx-smi
```

回显：

```text
mx-smi  version: 2.2.12

=================== MetaX System Management Interface Log ===================
Timestamp                                         : Wed Jul 22 17:55:49 2026

Attached GPUs                                     : 1
+---------------------------------------------------------------------------------+
| MX-SMI 2.2.12                      Kernel Mode Driver Version: 3.8.30           |
| MACA Version: 3.5.3.20             BIOS Version: 1.33.5.0                       |
|------------------+-----------------+---------------------+----------------------|
| Board       Name | GPU   Persist-M | Bus-id              | GPU-Util      sGPU-M |
| Pwr:Usage/Cap    | Temp       Perf | Memory-Usage        | GPU-State            |
|==================+=================+=====================+======================|
| 0     MetaX C500 | 0           Off | 0000:38:00.0        | 0%          Disabled |
| 57W / 350W       | 36C          P0 | 858/65536 MiB       | Available            |
+------------------+-----------------+---------------------+----------------------+

+---------------------------------------------------------------------------------+
| Process:                                                                        |
|  GPU                    PID         Process Name                 GPU Memory     |
|                                                                  Usage(MiB)     |
|=================================================================================|
|  no process found                                                               |
+---------------------------------------------------------------------------------+

End of Log
```

### C2. 系统日志

命令：

```bash
uname -a
cat /etc/os-release
lscpu
free -h
df -h
```

回显：

```text
原始环境记录保存了以下字段：
system=Linux
release=5.15.0-58-generic
machine=x86_64

cat /etc/os-release、lscpu、free -h、df -h 的完整标准输出未保存。
```

### C3. Python日志

命令：

```bash
which python3
python3 --version
python3 -m pip --version
```

回显：

```text
Python 3.10.10
Python build: packaged by conda-forge
Python compiler: GCC 11.3.0

which python3 和 python3 -m pip --version 的完整标准输出未保存。
setup.log 显示项目虚拟环境位于：
/data/mxmoe-adapt/.venv/lib/python3.10/site-packages
```

### C4. PyTorch与设备日志

命令：

```bash
python3 -c "import torch; print('torch=', torch.__version__); print('available=', torch.cuda.is_available()); print('count=', torch.cuda.device_count()); print('device=', torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"
```

回显：

```text
torch= 2.8.0+metax3.5.3.9
available= True
count= 1
device= MetaX C500
```

### C5. Triton日志

命令：

```bash
python3 -c "import triton; print('triton=', triton.__version__); print('path=', triton.__file__)"
```

回显：

```text
triton= 3.0.0+metax3.5.3.9
path= 未被原始环境记录单独保存
```

### C6. 已安装组件日志

命令：

```bash
python3 -m pip show torch triton flag-gems vllm vllm-metax
```

回显：

```text
Name: torch
Version: 2.8.0+metax3.5.3.9
---
Name: triton
Version: 3.0.0+metax3.5.3.9
---
Name: flag-gems
Version: 5.0.2
Vendor: metax
Source commit: 30bfcc0735b8ebd48bd7339ef752ec64df69663f
---
Name: vllm
Version: 0.17.0
---
Name: vllm-metax
Status: 未在原始包版本快照中发现独立发行包记录
```

### C7. 编译工具日志

命令：

```bash
gcc --version
g++ --version
cmake --version
```

回显：

```text
Python 构建信息记录的编译器：GCC 11.3.0

gcc --version、g++ --version、cmake --version 的完整标准输出未保存。
```

## D. 异常记录

```text
执行失败的命令：2026-07-23 尝试通过原SSH入口重新采集 C2、C3、C5、C7 完整回显
完整错误信息：Permission denied（临时密钥已不被该实例接受）
是否重试：是（WSL OpenSSH 与 Windows OpenSSH 各一次，均未进入实例）
是否安装或升级过软件：否
其他说明：本次仅整理仓库内已有实机记录，没有安装、升级或修改远端环境；未把SSH地址、端口或认证信息写入公开文件。
```

