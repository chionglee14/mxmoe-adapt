# C500开通信息与环境日志表

请直接复制本表填写。密码、私钥、Token不要填写或粘贴。

## A. 租赁前：镜像选择表

```text
【租赁平台】
平台名称：模力方舟
平台页面链接（如有）：

【硬件选项】
GPU完整名称：MetaX C500（沐曦曦云 C500）
单卡显存：64 GB
GPU数量：1
是否整卡独占：
CPU可选规格：Intel(R) Core(TM) i7-8550U
内存可选规格：128 GB
系统盘可选规格：30 GB
数据盘可选规格：500 GB

【可选镜像】
请把页面里所有与以下关键词有关的镜像完整名称粘贴过来：
MXMACA / PyTorch / mcPyTorch / Triton / vLLM-MetaX / FlagOS

镜像1：vLLM / 0.17.0 / Python 3.10 / maca 3.5.3.307
镜像2：
镜像3：
镜像4：

【访问方式】
是否支持SSH：是
是否支持上传文件：是
是否可以访问GitHub或Gitee：是
计费价格（元/小时）：4
```

已确认镜像为 `vLLM / 0.17.0 / Python 3.10 / maca 3.5.3.307`。整卡独占属性及其他候选镜像未获得页面完整名称，因此保留为空，不作推测。不要只写“最新版”，必须复制完整镜像Tag。

## B. 开通后：实例信息表

```text
【实例信息】
实例名称：
开通时间：
到期时间：
SSH别名（建议metax-c500）：
当前工作目录：

【注意】
不要填写SSH密码。
不要粘贴私钥。
不要粘贴平台Token。
```

## C. 开通后：只读环境日志

按顺序执行下列命令，把每段完整回显粘贴到对应位置。第一轮不要安装或升级任何软件。

### C1. GPU日志

命令：

```bash
mx-smi
```

回显：

```text
粘贴到这里
```

### C2. 系统日志

命令：

```bash
uname -a
cat /etc/os-release
lscpu
free -h
df -h
```

回显：

```text
粘贴到这里
```

### C3. Python日志

命令：

```bash
which python3
python3 --version
python3 -m pip --version
```

回显：

```text
粘贴到这里
```

### C4. PyTorch与设备日志

命令：

```bash
python3 -c "import torch; print('torch=', torch.__version__); print('available=', torch.cuda.is_available()); print('count=', torch.cuda.device_count()); print('device=', torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"
```

回显：

```text
粘贴到这里
```

### C5. Triton日志

命令：

```bash
python3 -c "import triton; print('triton=', triton.__version__); print('path=', triton.__file__)"
```

回显：

```text
粘贴到这里
```

### C6. 已安装组件日志

命令：

```bash
python3 -m pip show torch triton flag-gems vllm vllm-metax
```

回显：

```text
粘贴到这里
```

### C7. 编译工具日志

命令：

```bash
gcc --version
g++ --version
cmake --version
```

回显：

```text
粘贴到这里
```

## D. 异常记录

```text
执行失败的命令：
完整错误信息：
是否重试：否
是否安装或升级过软件：否
其他说明：
```

如果某个命令不存在，保留原始错误，不要自行安装，直接把日志发给Codex处理。
